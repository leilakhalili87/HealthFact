**Instruction:**

- Specify model and training parameters in `./src/setting.py`
- run `./src/MainCode.ipynb`


**Summary of the Code**

- `setting.py` has the model variables.
- `data_prep.py` includes the functions to preprocess the input files to 
                 PyTorch DataLoader.
- `bert_model.py` created the bert model.

